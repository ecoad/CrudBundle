<div id="widget-internal-news" class="widget">
	<div class="inside">
		<h3 class="title">Internal News</h3>
		<ul>
			{% for internalNewsItem in internalNewsItems %}
				<li>
					<h4>
						<a href="{{ path('BrowserCreativeCrudBundle_internalnewsitem_view', {'itemId': internalNewsItem.id,
							'itemSubject': internalNewsItem.subject | urlSlugify}) }}">
							{{ internalNewsItem.subject }}
						</a>
					</h4>
					{% if app.security.isGranted('ROLE_ADMIN') %}
						<a href="{{ path('BrowserCreativeCrudBundle_internalnewsitem_edit', {'itemId': internalNewsItem.id}) }}">Edit</a>
						<a onclick='return confirm("Are you sure?")?  true : false;'
							href='{{ path('BrowserCreativeCrudBundle_internalnewsitem_delete', {
								'itemId' : internalNewsItem.id,
								'returnRoute': 'BrowserCreativeCrudBundle_main_index'
							}) }}' title='Delete this message'>
							Delete
						</a>
					{% endif %}
					{{ internalNewsItem.body | raw }}
				</li>
			{% endfor %}
		</ul>
	</div>
</div>
