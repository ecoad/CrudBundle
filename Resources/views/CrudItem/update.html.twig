{% extends "HubSiteBundle:Site:base.html.twig" %}

{% if item.id == '' %}
    {% set internalTitle = 'Create Internal News Item' %}
{% else %}
    {% set internalTitle = 'Edit Internal News Item' %}
{% endif %}

{% block pageTitle %}{{ internalTitle }}{% endblock %}

{% block content %}
    <h2>{{ internalTitle }}</h2>
    {% if item.id == '' %}
        <a href='{{ path('BrowserCreativeCrudBundle_main_index') }}'
            title='Cancel and go back to the news page'>Back to the News</a>
    {% else %}
        <a href='{{ path('BrowserCreativeCrudBundle_internalnewsitem_view', {'itemId': item.id, 
            'itemSubject': item.subject | urlSlugify }) }}'
            title='Cancel and view the news item'>Back to the News Item</a>
    {% endif %}
    <form action="
        {% if item.id == '' %}
            {{ path('BrowserCreativeCrudBundle_internalnewsitem_new') }}
        {% else %}
            {{ path('BrowserCreativeCrudBundle_internalnewsitem_edit', {'itemId': item.id}) }}
        {% endif %}
    " method="post">

        {{ form_errors(form) }}

        {{ form_row(form.subject) }}
        {{ form_row(form.body) }}

        {{ form_rest(form) }}

        <input type="submit" name='Submit' value='Submit'/>
    </form>
{% endblock %}